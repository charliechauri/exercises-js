/**
 * @todo Create unit testing
 */
const hearsay = require('../src/hearsay');

describe(`${hearsay.name}()`, () => {
  const level = 1;
  const expectedOutput = [1];

  it(`Given ${level} hearsay(${level}) is equals to ${expectedOutput}`, () => {

  });
});
